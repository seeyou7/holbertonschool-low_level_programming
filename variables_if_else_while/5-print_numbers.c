#include <stdio.h>

/**
 * main - Prints the alphabet in lowercase and uppercase after.
 *
 * Return: Always 0.
 */
int main(void)
{
int x;
for (x = 0; x <= 9; x++)
{
printf("%d", x);
}
printf("\n");
return (0);
}
