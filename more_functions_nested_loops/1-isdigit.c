#include "main.h"

/**
 *_isdigit - a function that checks for digit from 0 to 9
 *@c: parameter of the function
 *
 *Return: 1 if c is digit returns 0 otherwise
 */

int _isdigit(int c)
{
	if (c >= 48 && c <= 57) /* '_ is for 0 and 57 is for 9 */
	{
	return (0);
	}
	else
	{
		return (1);
	}
}

