#include "main.h"

/**
 *mul - function that multiple a by b
 *@a: parameter of the function
 *@b:  parameter of the function
 *Return: Always (0)
 */

int mul(int a, int b)
{
	int mul;

	mul = (a * b);
	return (mul);
}
